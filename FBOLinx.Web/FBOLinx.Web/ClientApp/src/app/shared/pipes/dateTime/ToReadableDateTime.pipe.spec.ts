/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ToReadableDateTimePipe } from './ToReadableDateTime.pipe';

describe('Pipe: ToReadableDateTimee', () => {
  it('create an instance', () => {
    let pipe = new ToReadableDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
