/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NullOrEmptyDefaultPipe } from './NullOrEmptyToDefault.pipe';

describe('Pipe: NullOrEmptyDefaulte', () => {
  it('create an instance', () => {
    let pipe = new NullOrEmptyDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
